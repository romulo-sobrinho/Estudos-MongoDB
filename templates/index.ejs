<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirection</title>
</head>
<body>
  <form action="/" method="POST">
    <input class="field" type="text" name="title" placeholder="Title" value="<%=body.title%>">
    <input class="field" type="text" name="description" placeholder="Description" value="<%=body.description%>">
    <input class="field" type="url" name="url" placeholder="URL" value="<%=body.url%>">
    <button>Salvar</button>
  </form>

  <% if(error) { %>
    <h3><%= error.message  %></h3>
  <% } %>
</body>
</html>